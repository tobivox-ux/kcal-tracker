name: Build unsigned iOS ipa (for AltStore/SideStore sideload)

on:
  workflow_dispatch:
  push:
    branches:
      - claude/fitness-nutrition-tracker-init-e33doy

jobs:
  build-ipa:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Expo prebuild (generate native iOS project)
        run: npx expo prebuild --platform ios --no-install

      - name: Install CocoaPods
        run: |
          cd ios
          pod install

      - name: Find workspace and scheme
        id: xcode
        run: |
          cd ios
          WORKSPACE=$(ls -d *.xcworkspace | head -n 1)
          PROJECT=$(ls -d *.xcodeproj | grep -v Pods.xcodeproj | head -n 1)
          SCHEME=$(basename "$PROJECT" .xcodeproj)
          echo "workspace=$WORKSPACE" >> "$GITHUB_OUTPUT"
          echo "scheme=$SCHEME" >> "$GITHUB_OUTPUT"
          echo "Using workspace=$WORKSPACE scheme=$SCHEME"

      - name: Build unsigned .app
        run: |
          cd ios
          xcodebuild build \
            -workspace "${{ steps.xcode.outputs.workspace }}" \
            -scheme "${{ steps.xcode.outputs.scheme }}" \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            -destination "generic/platform=iOS" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            ARCHS=arm64 \
            ONLY_ACTIVE_ARCH=NO

      - name: Package .ipa
        run: |
          cd ios
          find build/Build/Products -maxdepth 3 -name "*.app"
          APP_PATH=$(find "build/Build/Products/Release-iphoneos" -maxdepth 1 -name "*.app" | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "No .app found under Release-iphoneos, falling back to a full search"
            APP_PATH=$(find build/Build/Products -maxdepth 3 -name "*.app" | head -n 1)
          fi
          echo "Found app at: $APP_PATH"
          test -d "$APP_PATH"
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -r kcal-tracker-unsigned.ipa Payload
          mv kcal-tracker-unsigned.ipa ..

      - uses: actions/upload-artifact@v4
        with:
          name: kcal-tracker-unsigned-ipa
          path: kcal-tracker-unsigned.ipa
          retention-days: 14
